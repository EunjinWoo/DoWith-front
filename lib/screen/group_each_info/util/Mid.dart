import 'package:flutter/material.dart';

class eachGroupinfoPage_body_mid extends StatelessWidget {
  const eachGroupinfoPage_body_mid({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.symmetric(horizontal: 10),
      child: Container(
        color: Color(0xFF606060),
      ),
    );
  }
}